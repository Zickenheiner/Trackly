const routes = {
  login: '/login',
  dashboard: '/',
  transactions: '/transactions',
  categories: '/categories',
  goals: '/goals',
  register: '/register',
  profile: '/profile',
};

export default routes;
